# 📺 如何打开控制台 - 快速指南

## 第 1 步：找到 MD.AI 应用窗口

```
┌────────────────────────────────────────┐
│  📝 MD.AI - untitled.md               │  ← 这是 MD.AI 应用窗口
├────────────────────────────────────────┤
│  [文件] [编辑] [视图]                 │
├────────────────────────────────────────┤
│                                        │
│  编辑器区域                            │
│                                        │
│                                        │
└────────────────────────────────────────┘
```

**注意**：不是 Cursor IDE 窗口，是单独的 MD.AI 应用窗口！

---

## 第 2 步：在 MD.AI 窗口中按快捷键

### macOS:
```
⌘ (Command) + ⌥ (Option) + I
```

**或者**:
```
⌘ (Command) + ⌥ (Option) + J
```

### Windows/Linux:
```
Ctrl + Shift + I
```

**或者**:
```
F12
```

---

## 第 3 步：开发者工具已打开

会看到类似这样的界面：

```
┌────────────────────────────────────────────────────────────┐
│  📝 MD.AI - untitled.md                                   │
├────────────────────────────────────────────────────────────┤
│  [文件] [编辑] [视图]                                     │
├──────────────────────────────┬─────────────────────────────┤
│                              │  ┌─ 开发者工具            │
│  编辑器区域                  │  │                         │
│                              │  │ [Elements] [Console]... │
│                              │  │                         │
│                              │  │ > console.log()         │
│                              │  │   [file.ts] Opening... │
└──────────────────────────────┴─────────────────────────────┘
                                    ↑
                              点击这个标签页
```

**或者**开发者工具可能在单独窗口：

```
┌─────────────────────────────┐  ┌──────────────────────────┐
│  📝 MD.AI                   │  │ 开发者工具               │
│                             │  │                          │
│  [文件] [编辑]              │  │ [Elements] [Console]     │
│                             │  │                          │
│  编辑器区域                 │  │ > console.log()          │
│                             │  │   [file.ts] Opening...   │
└─────────────────────────────┘  └──────────────────────────┘
    MD.AI 应用窗口                  开发者工具窗口
```

---

## 第 4 步：测试功能并查看日志

### 测试文件打开 (⌘O)

1. **在控制台中，确保没有过滤器**
2. **按 `⌘O` 打开文件**
3. **观察控制台输出**：

#### ✅ 正常情况：
```
[file.ts] Opening file dialog in Tauri...
[file.ts] Selected file: /Users/cgy/Documents/test.md
[file.ts] File read successfully, length: 1234
```

#### ❌ 错误情况示例 1 - 权限问题：
```
❌ Error: Permission denied: dialog:allow-open
   at Object.open (dialog.ts:45)
   at openFile (file.ts:19)
```

**解决**：权限配置问题，需要修改 `capabilities/default.json`

#### ❌ 错误情况示例 2 - 模块未找到：
```
❌ Error: Cannot find module '@tauri-apps/plugin-dialog'
   at openFile (file.ts:19)
```

**解决**：依赖未安装，运行 `npm install`

#### ❌ 错误情况示例 3 - Tauri 环境检测失败：
```
❌ TypeError: window.__TAURI__ is undefined
```

**解决**：可能在 Web 模式运行，需要用 `npm run tauri dev`

---

## 📋 快速检查清单

在测试任何功能前：

- [ ] ✅ MD.AI 应用窗口已打开（不是 Cursor IDE）
- [ ] ✅ 开发者工具已打开（`⌘⌥I`）
- [ ] ✅ "Console" 标签页已选中
- [ ] ✅ 控制台没有错误（红色文字）

---

## 🧪 测试所有功能

### 1. 测试打开文件 (⌘O)
```bash
[file.ts] Opening file dialog in Tauri...
[file.ts] Selected file: /path/to/file.md
[file.ts] File read successfully, length: 1234
```

### 2. 测试保存文件 (⌘S)
```bash
[file.ts] Saving file, currentPath: undefined
[file.ts] Saving to: /path/to/new-file.md
[file.ts] File saved successfully
```

### 3. 测试另存为 (⇧⌘S)
```bash
[file.ts] Save As dialog...
[file.ts] Save As path: /path/to/another-file.md
[file.ts] Save As completed successfully
```

---

## 🆘 遇到问题？

### 问题 1: 按快捷键没反应

**原因**：可能焦点不在 MD.AI 窗口
**解决**：
1. 点击 MD.AI 窗口确保激活
2. 再按 `⌘⌥I`

---

### 问题 2: 看不到开发者工具

**原因**：可能在单独窗口打开了
**解决**：
1. 按 `⌘+Tab` 切换应用
2. 查找 "开发者工具" 窗口
3. 或者关闭重新按 `⌘⌥I`

---

### 问题 3: 控制台全是错误

**症状**：红色错误满屏
**解决**：
1. 截图发给我
2. 或复制错误文本
3. 我会帮您修复

---

## 📞 快速联系

**如果文件对话框不弹出**，在控制台查找：
- 🔴 红色错误 = 代码错误
- 🟡 黄色警告 = 可能的问题
- ⚪ 白色日志 = 正常信息

**把错误信息告诉我**，格式：
```
错误信息：[完整的错误文本或截图]
操作步骤：[你做了什么]
预期结果：[应该发生什么]
实际结果：[实际发生了什么]
```

---

## 🎯 终极测试命令

在控制台中直接测试（粘贴这段代码）：

```javascript
// 测试 Tauri 环境
console.log('Tauri 环境:', '__TAURI__' in window ? '✅ 是' : '❌ 否');

// 测试文件打开功能
const testOpen = async () => {
  const { openFile } = await import('/src/lib/file.ts');
  const result = await openFile();
  console.log('测试结果:', result);
};

// 运行测试
testOpen();
```

按 Enter 执行，查看结果！
